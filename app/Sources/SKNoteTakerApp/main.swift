import SwiftUI
import SKNoteCore

// Placeholder entry point — replaced by the full SwiftUI app in the UI phase.
MainApp.main()

struct MainApp: App {
    var body: some Scene {
        WindowGroup {
            Text("SK Note Taker")
        }
    }
}
