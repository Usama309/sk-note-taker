# SK Note Taker — Test Report

**Date:** 2026-07-12
**Machine:** Apple M1 Max, macOS 26.4.1, Xcode 26.6 / Swift 6.3, Node 22.22
**Verdict:** ✅ All features implemented, tested, and verified working end-to-end.

## v1.1.0 addition — automatic meeting detection

- **14 unit tests** (40 Swift total): app registry (Zoom/Teams/WhatsApp/browser matching,
  dedicated-app-beats-browser), detection state machine (debounce, fire-once-per-call,
  re-arm after call ends, suppressed-while-recording, cooldown after dismiss, app-change
  resets streak), and the auto-detect setting (default on, legacy-JSON decode).
- **Hardware probe** (`sknote-audiocheck probe`): idle → `micInUse=false` → no notification;
  mic held + Teams running → `micInUse=true meetingApp=Microsoft Teams` → **WOULD NOTIFY**.
- **Live app, full flow**: with a Teams call active (mic in use), the app showed the
  **"You're in a Microsoft Teams meeting — Start taking notes?"** banner; clicking **Start
  Notes** created a meeting and recorded 17 segments / 32 s / 120 KB. ✅
- Fixed during this work: notification-auth `await` was blocking the detector from starting;
  detection now starts immediately and requests notification permission in the background.

## ⚠️ v1.0.1 correction — microphone

The original v1.0 report claimed the mic path worked; it did **not**. The live test's mic
channel produced pure silence (`rms=0.00000`) and I mis-read "chunks arriving" as success.
Root cause: `AVAudioInputNode.setVoiceProcessingEnabled(true)` turns the input into a duplex
VPIO unit that only delivers buffers while an output render chain runs — a capture-only app
has none, so the mic was silent (and in the CLI it failed init outright, error −10875).

**Fixed and verified for real:**
- Default to raw mic capture (no voice processing). Hardware test via `sknote-audiocheck mic`:
  `maxRMS 0.178`, all chunks non-silent. `both`: mic 0.19 + system 0.29 simultaneously.
- **Live app** (fresh TCC state, mic prompt granted): a real meeting produced **20 mic
  (Speaker 1) segments + 20 system segments** with matching transcribed speech, plus a
  300 KB recording. Speaker 1 was entirely absent before the fix.
- New safeguards so this can't silently recur: mic preflight request with a clear error on
  denial, first-run permission onboarding, Settings → Permissions panel, and **live per-channel
  level meters + a "no microphone audio" banner** in the recording view.
- New automated tests (RMS on signal vs silence, resampler energy, permission model, level-meter
  math) that assert the exact signature the bug had.

---

## Summary

| Suite | Tests | Result |
|---|---|---|
| Swift unit (stores, assembler, front-matter, **audio signal, permissions, level meter**) | 22 | ✅ pass |
| Swift pipeline integration (synthetic audio) | 1 | ✅ pass |
| Swift Claude-CLI integration (live) | 3 | ✅ pass |
| MCP server (spawned server, all 6 tools) | 26 | ✅ pass |
| Web app (all endpoints incl. PATCH + Range) | 16 | ✅ pass |
| **Automated total** | **68** | **✅ all pass** |
| Hardware audio (`sknote-audiocheck`, real mic + system) | 3 modes | ✅ signal present |
| Live app (manual, human-like via UI automation) | 13 flows | ✅ verified |

---

## 1. Automated tests

### Swift unit (`swift test`)
- Meeting/transcript/folder/chat/summary round-trips through the on-disk store.
- **Speaker rename is metadata-only**: renaming S2→Kainat leaves transcript segment keys
  untouched; rendered transcript shows "Kainat:".
- Malformed `meeting.json` is skipped, not fatal.
- Folder `resolveOrCreate` builds client→project hierarchy, idempotent + case-insensitive;
  removal cascades to children.
- Summary YAML front-matter round-trips (action items with/without owner, quotes escaped).
- **TranscriptAssembler**: mic tokens always S1; system tokens follow diarizer speakers
  (first-appearance → S2, S3…); consecutive same-speaker tokens coalesce; long gaps split;
  tokens outside diarizer segments snap to nearest; volatile results ignored.

### Swift pipeline integration (`SKNOTE_INTEGRATION=1`)
Synthetic 22 s meeting: `system.wav` (Samantha + Daniel voices) + `mic.wav` (Reed voice),
generated by `say`. Fed through `FileAudioSource` → full live pipeline:
- Transcript non-empty; system speech ("launch/checklist") and mic speech ("ship/version
  one") both transcribed.
- Mic segments all S1; **≥2 distinct diarized system speakers**.
- Rename S2→Kainat reflected in rendered transcript.
- `durationSec` reflects audio length; `recording.m4a` written with real data.

### Claude CLI integration (`SKNOTE_AI=1`, live subscription)
Fixture 3-speaker meeting (Saqib/Kainat/Rick):
- **Summary** extracts action items (referencing real commitments), decisions (the launch
  call), things to remember.
- **Q&A** "What did Kainat commit to, and by when?" → mentions Wednesday assets + Friday
  announcement.
- **Auto-categorization** → matches existing "Acme Corp" client folder, confidence > 0.3.

### MCP server (`npm test`, 26 tests)
Spawns the built server via MCP SDK `Client` + `StdioClientTransport` against a fixture data
dir; exercises all 6 tools (`list_meetings`, `get_meeting`, `get_transcript` with name
resolution, `get_summary` with front-matter parse, `search_meetings` with snippets,
`list_folders`). Missing dir → empty; malformed JSON → skipped with stderr warning.

### Web app (`npm test`, 16 tests)
Spawns the real Express server on a random port against throwaway fixtures; hits every
endpoint over HTTP including both PATCH endpoints (verifies atomic write landed, no temp
files left), audio Range/206, front-matter parsing, SPA fallback, MIME types.

---

## 2. Cross-component interop (real data)

The Swift pipeline wrote a real meeting to a shared dir (`SKNOTE_PIPELINE_OUT`); then:
- **MCP server** read it: `list_meetings` returned the meeting with `durationSec` 22.16,
  `get_transcript` resolved S2→Kainat, `search_meetings` "checklist" hit the transcript. ✅
- **Web API** read it: `/api/meetings`, `/transcript` (resolved names), `/audio` (206 with
  `content-range: bytes 0-1023/90391`), `PATCH /speakers` (S3→Daniel persisted atomically),
  `/folders`. ✅
- **MCP registered** with Claude Code: `claude mcp add` → "✔ Connected". ✅

---

## 3. Live macOS app (human-like verification)

Built signed `SK Note Taker.app`, launched, drove via UI automation (accessibility + HID):

| # | Flow | Result |
|---|---|---|
| 1 | Launch → branded empty state ("Ready when you are") | ✅ |
| 2 | Grant Microphone permission (TCC prompt) | ✅ allowed |
| 3 | Grant System Audio Recording permission | ✅ allowed |
| 4 | Start Meeting → live transcription of system audio played through speakers | ✅ word-accurate, real-time volatile→final |
| 5 | **Multi-speaker diarization on live audio** | ✅ 2 voices → S2/S3/S2 correctly attributed |
| 6 | End Meeting → final pass, transcript + m4a saved, state=complete | ✅ |
| 7 | Generate Summary (in-app, via Claude CLI) | ✅ action items/decisions/remember/TL;DR rendered |
| 8 | Name speakers (S2→Kainat, S3→Daniel) via Speakers sheet | ✅ persisted to meeting.json |
| 9 | Transcript tab shows named, color-coded speakers | ✅ "Kainat"/"Daniel" bubbles |
| 10 | Chat: "What did Kainat say about check-ins?" | ✅ "At [00:19], Kainat said: '…weekly check ins on Friday mornings.'" |
| 11 | Auto-categorization → "Website Redesign" folder | ✅ meeting filed, folder in sidebar |
| 12 | Web view (Chrome): sidebar, meeting cards, transcript bubbles, audio playback | ✅ playback reached readyState 4, currentTime advanced |

Screenshots: `assets/screenshots/{meeting,summary,transcript,speakers,chat}.png`.

---

## 4. Bugs found and fixed during testing

1. **Per-chunk `AVAudioConverter`** corrupted audio at chunk boundaries → garbled ASR.
   Fixed with persistent converters.
2. **Hardened-runtime signing** without `com.apple.security.device.audio-input` silently
   denied the mic (no prompt). Entitlement added.
3. **System-tap IOProc** read the physical output device's silent stream, not the tap's.
   Now selects the ABL buffer matching the tap format's channel count.
4. **ASR finals dropped** at meeting end (consumer task cancelled instead of drained).
5. **Diarization under-split** on live audio (threshold 0.7 merged distinct speakers;
   macOS output processing compresses embedding distances). Lowered to 0.6 after a sweep on
   live captures; verified it doesn't over-split clean audio.
6. **`durationSec` = 0** for file sources; now derived from chunk timestamps.
7. **Claude CLI cwd** inherited the app's, triggering Desktop-access prompts; pinned to the
   data dir.

All re-tested green after fixing.

---

## 5. Known limitations (documented, v2 backlog)

- Speaker naming during a live meeting works, but the summary is a snapshot — regenerate
  after renaming to propagate names into the summary prose.
- No calendar auto-detection, templates, sharing, or cloud sync in v1 (README backlog).
- Diarization threshold is tuned for meeting/system audio; unusual setups may need tuning.
