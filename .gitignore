# Build artifacts
.build/
build/
DerivedData/
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
dist/

# Node
node_modules/
*.tsbuildinfo

# Local dev artifacts
logs/
tests/tmp/
.DS_Store

# Runtime data (never commit meeting data)
*.sqlite
*.sqlite-wal
*.sqlite-shm

# Secrets
.env
.env.*
supabase/.env
supabase/.temp/
**/service-role-key*
*.db-password

# Ralph context management
.ralph/
.ralphrc

# Local convenience symlinks into the project folder (private data — never commit)
#   meeting-data -> ~/Library/Application Support/SKNoteTaker  (recordings + transcripts)
#   logs-live    -> ~/Desktop/SK Note Taker Logs               (runtime logs)
meeting-data
logs-live
